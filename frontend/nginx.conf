server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # React Router (history API) のクライアントサイドルーティング対応
    location / {
        try_files $uri $uri/ /index.html;
    }
}
